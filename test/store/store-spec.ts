import { expect } from 'chai';
import 'mocha';
import { StoreSync } from "../../src/Store";

describe("Store Test", () => {
    it('Default Test', () => {
        var store = new StoreSync()
    });
});